# 🩸 铁律：AI agent 绝不主动合并 / 关闭 PR

> 面向所有进入本仓库的 AI agent 与人类协作者。**动手之前先读完这页。**
>
> 本页不是推测，是 `sunccchengze/turbine-blade-ai-platform` 项目里
> 由承泽与历代 agent 实战积累、写进 `HANDOFF.md` 的原始记录。

## 一、铁律原文

> **绝不主动合并 PR。** 🩸 Arena 会在 PR **合并/关闭**后**立刻关闭本会话的远程通道**，
> 之后所有 `push` / `gh` 全失败。
> → 合并 PR 只能是会话的**最后一个动作**，或留给承泽在 GitHub 网页点。
> **要继续干活就让 PR 开着。**

配套的第一条铁律（同样重要）：

> **推送优先于一切。** 每完成一个可交付单元，立刻 `commit` + `push`，**绝不攒提交**。
> **未推送的提交 = 不存在的提交**（`af73fdc` 教训）。

## 二、看清三个容易搞错的点

**1. 触发条件是「合并**或**关闭」**——不只是合并。把 PR 关掉同样会关闭远程通道。

**2. 后果是远程通道被关**，不是含糊的"链接断了"：之后 `git push`、`gh` 全部失败，
会话无法继续干活。此时**尚未推送的提交就会永久丢失**。

**3. 不能用「分支还在不在」判断**。分支通常好端端地在远端，通道照样已经关闭。
也与 GitHub 的 `delete_branch_on_merge` 设置无关（该仓为关闭状态，一样出事）。

## 三、正确做法

```bash
# 推荐：内容进 main 用快进推送，不碰 PR，通道不受影响
git push origin <你的分支>:main

# PR 可以开、可以推进，但 agent 不要去合、也不要去关
gh pr create ...        # 开 PR 没问题
gh pr merge ...         # 🩸 会关闭远程通道
gh pr close ...         # 🩸 同样会关闭远程通道
```

快进推送前先自检 `main` 是否为你分支的祖先：

```bash
git fetch origin main
git merge-base --is-ancestor origin/main HEAD && echo "FF 安全" || echo "需要先 rebase"
```

**要合并 PR，只有两种安全时机**：
1. 作为会话的**最后一个动作**（后面不再需要任何 push / gh）；
2. 交给承泽在 GitHub 网页上点。

## 四、真实代价：Day 19 事故

原始记录：

> Day 19 提交 `43b461d` 曾未推送（**旧会话在 PR #3 合并后被关闭**）。
> 本会话已用承泽粘贴的 `D19_RECOVERY.md`（README 全文 + 3 个代码文件改动）
> 重建为 `1839aa5`，4 项验证全过并推送。

即：PR 合并 → 会话通道关闭 → 未推送的提交随之消失 → 只能靠人工粘贴内容重建。
这正是铁律 1「未推送的提交 = 不存在的提交」的由来。

## 五、前兆信号（很有用）

> 推送前常先撞一次 gnutls TLS 报错，看着像抖动，**其实是会话将关闭的前兆**；
> 别机械重试超 2–3 次。

看到反复的 TLS / 网络报错时，别当成偶发抖动死磕重试。**优先把手上的工作
commit 并尝试推送**，推不上去就立刻导出 patch 存档并如实上报——
不要静默跳过、不要假装成功（铁律 3）。

## 六、来源

`sunccchengze/turbine-blade-ai-platform` → `HANDOFF.md`
（§0.-1 五条铁律 第 2 条、§0.1 Day 19 恢复、§9 沙盒坑与教训 #12 #13）。
该文件是历代会话交接总文档，本页为其中通用部分的摘录。

## 七、本仓实际做法

`main` 上的全部归档内容，均通过 `git push origin arena/01a060a9-ai:main`
快进推送送达，**全程未创建、未合并、未关闭任何 PR**。工作分支与会话通道均完好。
