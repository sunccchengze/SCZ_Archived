[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "scz-second-brain"
version = "0.1.0"
description = "Obsidian-first local-first personal knowledge system"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
second-brain = "second_brain.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
